package jta.objects.level;

import polymod.hscript.HScriptedClass;
import jta.objects.level.Player;

/**
 * A script that can be tied to `Player`.
 */
@:hscriptClass
class ScriptedPlayer extends Player implements HScriptedClass {}
